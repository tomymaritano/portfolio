CONTENT_API_TOKEN=
GITHUB_TOKEN=
GITHUB_REPO=tomymaritano/portfolio

# Optional: Ghost as the writing CMS. Local MDX stays the archive.
# Ghost(Pro) Starter is enough. Create a Custom Integration for the Content API key.
# Webhook: post.published / post.updated / post.deleted → POST /api/ghost/revalidate?secret=...
GHOST_URL=
GHOST_CONTENT_API_KEY=
GHOST_WEBHOOK_SECRET=
